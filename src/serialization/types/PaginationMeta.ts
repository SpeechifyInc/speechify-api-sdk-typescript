/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as serializers from "../index";
import * as Speechify from "../../api/index";
import * as core from "../../core";

export const PaginationMeta: core.serialization.ObjectSchema<serializers.PaginationMeta.Raw, Speechify.PaginationMeta> =
    core.serialization.object({
        nextCursor: core.serialization.property("next_cursor", core.serialization.string().nullable()),
        hasMore: core.serialization.property("has_more", core.serialization.boolean()),
    });

export declare namespace PaginationMeta {
    export interface Raw {
        next_cursor: string | null;
        has_more: boolean;
    }
}
