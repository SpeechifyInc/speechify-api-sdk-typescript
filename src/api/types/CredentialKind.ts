/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 * Discriminates the auth flow a credential carries. The matching
 * `config.<kind>` block is the one that must be populated.
 */
export type CredentialKind = "oauth2_client_credentials" | "oauth2_jwt" | "basic" | "bearer" | "custom_headers";
export const CredentialKind = {
    Oauth2ClientCredentials: "oauth2_client_credentials",
    Oauth2Jwt: "oauth2_jwt",
    Basic: "basic",
    Bearer: "bearer",
    CustomHeaders: "custom_headers",
} as const;
