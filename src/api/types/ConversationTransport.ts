/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 * How the caller reached the agent. `web` is the browser /
 * SDK realtime path; the `sip_*` and `phone` variants come
 * from the telephony stack.
 */
export type ConversationTransport = "web" | "phone" | "whatsapp" | "sip_inbound" | "sip_outbound";
export const ConversationTransport = {
    Web: "web",
    Phone: "phone",
    Whatsapp: "whatsapp",
    SipInbound: "sip_inbound",
    SipOutbound: "sip_outbound",
} as const;
