/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Speechify from "../index";

/**
 * Kind-specific credential payload. Exactly one block is
 * populated — the one named by the credential's `kind`. The
 * block IS the secret; it is echoed back decrypted on reads.
 */
export interface CredentialConfig {
    oauth2ClientCredentials?: Speechify.OAuth2ClientCredentialsConfig;
    oauth2Jwt?: Speechify.OAuth2JwtConfig;
    basic?: Speechify.BasicAuthConfig;
    bearer?: Speechify.BearerAuthConfig;
    customHeaders?: Speechify.CustomHeadersConfig;
}
