/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Speechify from "../index";

/**
 * Payload of GET /v1/knowledge-bases/documents/{docId}. Extends
 * the list-view document with a bounded content preview, the
 * list of dependent agents, and (for url-sourced docs) the
 * auto-refresh state.
 */
export interface KnowledgeBaseDocumentDetail extends Speechify.KnowledgeBaseDocument {
    contentPreview: string;
    previewTruncated: boolean;
    dependentAgents: Speechify.DependentAgent[];
    refresh?: Speechify.RefreshConfig;
}
