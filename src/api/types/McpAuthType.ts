/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 * Auth scheme the worker uses against the MCP endpoint. The
 * secret half of `bearer` and `oauth2_client_credentials` is
 * write-only on the wire; reads carry only the `_set` markers.
 */
export type McpAuthType = "none" | "bearer" | "oauth2_client_credentials";
export const McpAuthType = {
    None: "none",
    Bearer: "bearer",
    Oauth2ClientCredentials: "oauth2_client_credentials",
} as const;
