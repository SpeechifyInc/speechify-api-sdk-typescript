/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Speechify from "../../../index";

/**
 * One execution of a test. `result` is populated when `status`
 * reaches a terminal state (`passed`, `failed`, or `error`).
 * See `TestRunResult` for the shape.
 */
export interface AgentTestRun {
    id: string;
    testId: string;
    agentId: string;
    status: Speechify.tts.TestRunStatus;
    startedAt?: Date | null;
    completedAt?: Date | null;
    /** Populated on terminal status only. */
    result?: Speechify.tts.TestRunResult;
    /** Human-readable error message when status is `error`. */
    error?: string;
    createdAt: Date;
}
