/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Speechify from "../../../../index";

/**
 * @example
 *     {
 *         name: "Greet caller by name",
 *         description: "Agent should use the caller's name when it is provided.",
 *         type: "reply",
 *         config: {
 *             context: "Hi, I am Alice. I need help with my order.",
 *             successCriteria: "The agent addresses Alice by name in its response.",
 *             successExamples: ["Hi Alice! I would be happy to help with your order.", "Sure thing, Alice - let me look that up for you."],
 *             failureExamples: ["Hello! How can I help you today?", "I can help you with that order."]
 *         }
 *     }
 *
 * @example
 *     {
 *         name: "Reservation party size and time",
 *         description: "Agent calls create_reservation with the right party size and time.",
 *         type: "tool",
 *         config: {
 *             context: "Book a table for 2 at 7pm tonight.",
 *             expectedTool: "create_reservation",
 *             parameterChecks: [{
 *                     path: "party_size",
 *                     mode: "exact",
 *                     expected: "2"
 *                 }, {
 *                     path: "time",
 *                     mode: "regex",
 *                     expected: "^19:00"
 *                 }, {
 *                     path: "date",
 *                     mode: "llm",
 *                     criteria: "is today or a reasonable interpretation of tonight"
 *                 }]
 *         }
 *     }
 *
 * @example
 *     {
 *         name: "Order cancellation - full flow",
 *         description: "Agent handles a full cancellation conversation without escalation.",
 *         type: "simulation",
 *         config: {
 *             scenario: "You are a customer who placed order ORD-8821 three days ago and wants to cancel it. You are polite but firm. Do not accept partial refunds.",
 *             maxTurns: 8
 *         }
 *     }
 *
 * @example
 *     {
 *         name: "Order lookup by id",
 *         description: "Agent looks up the order id supplied via test variables.",
 *         type: "tool",
 *         config: {
 *             context: "Look up order {{order_id}} for {{customer_name}}",
 *             expectedTool: "lookup_order",
 *             parameterChecks: [{
 *                     path: "order_id",
 *                     mode: "exact",
 *                     expected: "\"{{order_id}}\""
 *                 }]
 *         },
 *         variables: {
 *             "order_id": "ORD-123",
 *             "customer_name": "Alice"
 *         }
 *     }
 */
export interface CreateAgentTestRequest {
    /** Short human-readable label for the test. */
    name: string;
    /** Optional longer description of what this test verifies. */
    description?: string;
    type: Speechify.TestType;
    /** Type-specific configuration. Must match the shape for the given `type`. */
    config: Speechify.CreateAgentTestRequestConfig;
    /** Optional tool-mocking config applied during every run of this test. */
    toolMockConfig?: Speechify.ToolMockConfig;
    /**
     * Per-test variable values substituted into string fields of the
     * config at run-start. Keys use the same rules as agent-level
     * `DynamicVariable` keys.
     */
    variables?: Record<string, unknown>;
    /**
     * Prefixed wire identifier (`folder_<26 char Crockford base32>`)
     * of the folder to place the test in. Omit / null for root.
     */
    folderId?: string | null;
    /**
     * Optional list of additional agents this test should also run
     * against. The owner agent (path param) is always attached
     * implicitly.
     */
    attachedAgentIds?: string[];
}
